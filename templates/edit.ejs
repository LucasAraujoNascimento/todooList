<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Todo List</title>
    
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>

    <div class="container mt-5">

        <h1 class="text-center mb-5">To-do List</h1>

        <form action="/" method="POST">
            <div class="form-group">
                <input type="text" name="task" class="form-control" placeholder="Task" required>
            </div>
            <div class="form-group">
                <input type="text" name="description" class="form-control" placeholder="Write a description" required>
            </div>
            <button type="submit" class="btn btn-primary btn-block">Submit task</button>
        </form>

        <hr>

        <%for(let i=0; i < tasks.length; i++){%>

            <div class="card mb-3">
                <div class="card-body">
                    <form method="POST" action="/?_method=DELETE">
                        <h3><%=tasks[i].task%></h3>
                        <p><%=tasks[i].description%></p>
                        <p><small><strong><%=tasks[i].date%></strong></small></p>
                        <input name="id" value="<%=tasks[i]._id%>" hidden>
                        <button type="submit" class="btn btn-danger">Excluir</button>
                        <a href="/edit/<%=tasks[i]._id%>" class="btn btn-secondary">Edit Task</a>
                    </form>
                </div>
            </div>

        <% } %>

    </div>
</body>
</html>